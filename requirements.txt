# pip requirements for gridfinity_build123d.
#
#     python -m pip install -r requirements.txt
#     python -m pip install -e .
#
# Versions are those verified to work together. See environment.yml for the
# conda route, which is what this library is developed in on Windows.
#
# Installing build123d from PyPI pulls the OCP CAD kernel as a large binary
# wheel. The conda route takes ocp from conda-forge instead. Use one or the
# other, not both in the same environment.

# --- runtime -----------------------------------------------------------------
# Required to import and use the library.
build123d==0.9.1
bd_warehouse @ git+https://github.com/gumyr/bd_warehouse@48c507baac94231e2eb2c4032848719660f964dc

# --- documentation -----------------------------------------------------------
# Required for `sphinx -b html docs/source docs/build/html` and for the doctests
# in the documentation. Pinned to match docs/requirements.txt, which Read the
# Docs uses.
Sphinx==8.1.3
sphinx-rtd-theme==3.0.2
sphinx_design==0.6.1

# --- tests and linting -------------------------------------------------------
# Required to run the test suite and the README code blocks. The last three are
# listed in the dev group of pyproject.toml but are not part of the environment
# the library was verified in, so they are deliberately left unpinned.
pytest==9.1.1
parameterized==0.9.0
pytest-markdown-docs==0.9.2
ruff==0.16.7
coverage
vulture
basedpyright

# --- development -------------------------------------------------------------
# Optional. ocp_vscode previews models in an OCP CAD Viewer window while you
# edit a script; debugpy attaches a debugger.
ocp_vscode==4.0.1
debugpy==1.8.21
